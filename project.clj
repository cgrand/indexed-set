(defproject net.cgrand.indexed-set "0.5.0"
  :description "A set implementation which supports unicity constraints and
                maintains summaries (indexes)."
  :dependencies [[org.clojure/clojure "[1.2.0,)"]])
